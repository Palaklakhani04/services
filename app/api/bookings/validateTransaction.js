export async function verifyTransaction(transactionId) {
    // Simulating verification (replace with real API request)
    const validTransactions = ["TXN12345", "TXN67890"]; // Example valid transactions
    return validTransactions.includes(transactionId);
  }
  